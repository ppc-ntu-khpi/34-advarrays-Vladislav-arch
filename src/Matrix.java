/**
 * Інтерфейс {@code Matrix} містить метод, що повертає двовимірний масив.
 */
public interface Matrix {

    /**
     * Повертає двовимірний масив.
     *
     * @return двовимірний масив.
     */
    int[][] getMatrix();
}
